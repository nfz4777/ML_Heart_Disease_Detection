# ML_Heart_Disease_Detection
With 72.6% accuracy, the model can detect whether a person has heart disease or not based on different features through an Ensemble method called Random Forest Classifier. 
